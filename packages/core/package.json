{
  "name": "@kitland/core",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-neutral domain logic for Kitland tools",
  "files": [
    "src"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit --pretty false",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint src --vitest-plugin --deny-warnings"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
