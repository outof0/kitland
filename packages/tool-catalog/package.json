{
  "name": "@kitland/tool-catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Tool definitions, schemas, and catalog metadata for Kitland",
  "files": [
    "src"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit --pretty false",
    "test": "vitest run",
    "release:verify": "vitest run --config vitest.release.config.ts",
    "lint": "oxlint src --vitest-plugin --deny-warnings"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
